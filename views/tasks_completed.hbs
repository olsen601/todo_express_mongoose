<div class="tasklist">
  {{#each tasks}}
    <li class="list"><a href="task/{{this._id}}">{{this.text}}</a> Created: {{formatDate this.dateCreated}}, Completed: {{formatDate this.dateCompleted}}</li>
  {{else}}
    <p>No tasks have been completed!</p>
  {{/each}}
</div>

<div class="deleteDone">
  {{#if tasks}}
    <form action="/deleteDone" method="POST">
      <input class="delete_done_button" type="submit" value="Delete All">
    </form>
  {{/if}}
</div>
